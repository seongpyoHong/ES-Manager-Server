apiVersion: v1
kind: Service
metadata:
  name: {{ include "elasticsearch.name" . }}-client
  labels:
    component: elasticsearch
spec:
  selector:
    component: elasticsearch
    role: client
  type: LoadBalancer
  ports:
  - name: http
    port: 9200
    protocol: TCP
